/**
 * Git / GitHub — atsauce (PowerShell).
 * Repozitorija sakne: vadības sistēmas arhitektura (par vienu līmeni virs šīs mapes).
 *
 * git add .
 * git commit -m "Apraksts par izmaiņām"
 * git push origin main
 *
 * Pēc pull — pārlūkā Ctrl+F5.
 * Deploy instrukcijas: README.md, docs/DEPLOY_IESTADE.md
 */
